#include "main.h"

/**
 * _abs - prints an absolute value of an integer
 * @a: The integer to be computed
 * Return: the abs value
 */
int _abs(int a)
{
	if (a >= 0)
		return (a);
	else
		return (-a);
}
